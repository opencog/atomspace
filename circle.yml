machine:
    services:
        - docker

dependencies:
    override:
        - docker info
        - docker pull opencog/cogutils
        - docker images opencog/cogutils

test:
    override:
        - docker run -v $PWD:/atomspace -it opencog/cogutils /bin/sh -c "cd /atomspace && /tmp/octool -bet":
            parallel: true
