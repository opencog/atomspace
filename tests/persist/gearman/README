

In order for this test to pass, you must install the package
`gearman-job-server` (in Debian, Ubuntu) and also make sure that the 
job server is actually running: viz, 

```
  sudo service gearman-job-server status
```
and, if necessary,
```
  sudo service gearman-job-server start
```
